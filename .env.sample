# Tushare Pro API Token
# Get yours at: https://tushare.pro/register
TUSHARE_TOKEN=your_token_here

# Optional delay (seconds) between Tushare API calls. Defaults to 0.5.
# Set to 0 only when your broker/VIP endpoint has no client-side rate limit.
# TUSHARE_RATE_DELAY=0.5
